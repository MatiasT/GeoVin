{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "typeRoots": ["node_modules/@types"],
    "outDir": "../out-tsc/app",
    "types": ["leaflet","leaflet.markercluster"]
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ]
}
